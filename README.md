# Aria Homebrew Tap
Homebrew formula for the [Aria](https://github.com/egranata/aria/) programming language. To get started tap the formula:
```
brew tap arialang/aria
```
Then install:
```
brew install aria
```
